[pytest]
markers =
    unit: fast tests without real models or production indexes
    integration: component integration tests using temporary or production indexes
    regression: fixed retrieval-quality regression corpus
    gpu: requires a CUDA-capable GPU
    slow: loads production models or full indexes
